# Qupid バックエンド環境変数設定
# このファイルを .env にコピーして使用してください

# ============================================================
# データベース設定
# ============================================================
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/mydatabase

# Docker Compose使用時（推奨）
# DATABASE_URL=postgresql+asyncpg://user:password@db:5432/mydatabase

# ============================================================
# セキュリティ設定
# ============================================================
# 🔴 重要: 本番環境では必ず変更してください！
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=HPdYeUvvuDMUYadvBkiYstDOtAgQjnwd2DDCC-F8Lo0

# アクセストークンの有効期限（分）
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ============================================================
# アプリケーション設定
# ============================================================
APP_NAME=Qupid API
APP_ENV=development

# 許可するメールドメイン（九州大学限定）
ALLOWED_EMAIL_DOMAIN=s.kyushu-u.ac.jp

# ============================================================
# メール送信設定
# ============================================================
# 開発環境では false に設定（認証コードがコンソールに出力されます）
ENABLE_EMAIL=false

# 本番環境でメール送信を有効にする場合
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=kudai.qupid@gmail.com
SMTP_PASSWORD=your-app-password-here
FROM_EMAIL=noreply@qupid.com

# ============================================================
# CORS設定（本番環境）
# ============================================================
# カンマ区切りで複数のオリジンを指定可能
# ALLOWED_ORIGINS=https://qupid.vercel.app,http://localhost:3000
